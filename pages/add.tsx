import { NextPage } from "next";
import UploadForm from "../components/UploadForm";

const add:NextPage = () => {
  return(
    <div>
      <h1>Upload Form</h1>
      <UploadForm />
    </div>
  )

}

export default add;
